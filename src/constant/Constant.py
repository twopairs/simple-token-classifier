import os

class Constant:

    RESOURCE_YAML_PATH = "../config/resource.yaml"
    CONFIG_YAML_PATH = "../config/config.yaml"

    RESOURCE_DIR_PATH = "../resource"
    OUT_DIR_PATH = "../out"

    ORDER = "order"
    ANYORDER = "anyorder"
    COMBINATION = "combination"

    YAML_NAME_DIMENSION = "dimension.yaml"
    YAML_NAME_BRAND = "brand.yaml"

    MAX_LOOKAHEAD = 5
